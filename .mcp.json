{
  "mcpServers": {
    "playwright": {
      "type": "stdio",
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-playwright",
        "--base-url",
        "http://localhost:3000"
      ],
      "env": {
        "BROWSER": "chromium"
      }
    }
  }
}
